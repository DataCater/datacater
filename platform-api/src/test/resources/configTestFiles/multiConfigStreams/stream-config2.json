{
  "name": "stream-config2",
  "kind": "STREAM",
  "metadata" : {
    "labels" : {
      "app.datacater.io/test": "test-config"
    }
  },
  "spec": {
    "kafka": {
      "bootstrap.servers": "localhost:9092"
    }
  }
}
