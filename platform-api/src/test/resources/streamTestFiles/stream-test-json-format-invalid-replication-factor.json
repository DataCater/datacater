{
  "name": "testJson",
  "spec": {
    "kafka": {
      "bootstrap.servers": "localhost:9092",
      "topic": {
        "config": {
        },
        "replication.factor": "42"
      }
    },
    "kind": "KAFKA"
  }
}
