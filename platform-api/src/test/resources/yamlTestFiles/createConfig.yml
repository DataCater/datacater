---
name    : stream-config
kind    : STREAM
metadata:
  labels:
    app.datacater.io/name: stream-config
spec    :
  kafka:
    bootstrap.servers: localhost:9092
    topic:
      num.partitions: '4'
      replication.factor: '1'
